
/** File has been generated by TypeWriter. Modifications will be overriden when the template is rendered */
// @ts-ignore
import * as moment from 'moment';
import InterfaceConstructor from '../InterfaceConstructor';

interface TableRequestFilterDTO { 
    columnProp: string | null;
    filter: string | null;
}
const TableRequestFilterDTO: InterfaceConstructor<TableRequestFilterDTO> = {
    create: (initValues?: {} | null | undefined) => {
        return Object.assign(
        {
            columnProp: null,
            filter: null,
        },
        initValues);
    }
};

export default TableRequestFilterDTO;
