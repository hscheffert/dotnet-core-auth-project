
/** File has been generated by TypeWriter. Modifications will be overriden when the template is rendered */
// @ts-ignore
import * as moment from 'moment';
import InterfaceConstructor from '../InterfaceConstructor';

interface WeatherForecastDTO { 
    date: moment.Moment | string;
    temperatureC: number;
    temperatureF: number;
    summary: string | null;
}
const WeatherForecastDTO: InterfaceConstructor<WeatherForecastDTO> = {
    create: (initValues?: {} | null | undefined) => {
        return Object.assign(
        {
            date: new Date(0).toISOString(),
            temperatureC: 0,
            temperatureF: 0,
            summary: null,
        },
        initValues);
    }
};

export default WeatherForecastDTO;
